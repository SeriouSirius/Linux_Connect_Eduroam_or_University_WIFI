# 本指南用于解决 Linux 用户无法连接 **eduroam** 全球漫游网络或 **HKMU** 校园网络的问题。

[English](README.md) | [繁體中文](README.zh-TW.md) | [簡體中文](README.zh-CN.md)

---

## 1. Eduroam Linux 连接修复指南 (非官方修复方案)

eduroam 的配置要求比普通校园网更严格，且经常遇到 Linux 默认协议版本过新、证书校验无法绕过等问题。本脚本通过强制指定兼容模式来解决这些问题。

### 适用对象
*   HKMU 学生/教职员工 (使用 `sxxxxxx@hkmu.edu.hk`)
*   其他高校使用 eduroam 的 Linux 用户 (只需替换账号后缀即可)

### ⚡ 核心修复原理
1.  **PEAP Version 0**: 强制降级协议版本，解决旧式 Radius 服务器握手失败的问题。
2.  **No CA Cert**: 绕过 Linux 图形界面强制要求上传证书的限制。
3.  **Password Flags**: 禁用密钥环存储，防止因权限问题导致的无限密码弹窗。

### 🚀 快速连接命令

请在终端 (Terminal) 中**完整复制**以下命令块。

**⚠️ 注意：运行前请手动修改命令中的 `您的学号@hkmu.edu.hk` 和 `您的密码`。**

```bash
# 1. 删除旧的失败配置 (防止冲突)
nmcli connection delete eduroam 2> /dev/null

# 2. 创建兼容性极佳的 eduroam 连接
# 注意：eduroam 的 identity 必须包含完整的域名后缀 (如 @hkmu.edu.hk, @polyu.edu.hk 等)
sudo nmcli con add \
  type wifi \
  con-name "eduroam" \
  ssid "eduroam" \
  wifi-sec.key-mgmt wpa-eap \
  802-1x.eap peap \
  802-1x.phase1-auth "peapver=0" \
  802-1x.phase2-auth mschapv2 \
  802-1x.identity "s1234567@hkmu.edu.hk" \
  802-1x.password "您的密码" \
  802-1x.password-flags 0 \
  802-1x.system-ca-certs no

# 3. 启动连接
nmcli con up eduroam
```

---

## 2. HKMU Wi-Fi Linux 连接指南 (非官方修复方案)

### 🔴 问题现象
*   点击连接后一直转圈，最后断开。
*   反复弹窗要求输入密码，即使密码正确。
*   图形界面中「储存」按钮为灰色，强制要求上传 CA 证书。
*   系统日志 (`journalctl`) 显示 `Association took too long` 或 `secrets were required but not provided`。

### 🟢 原因分析
1.  **协议版本不匹配**：学校的 RADIUS 服务器使用较旧的 **PEAP v0** 协议，而现代 Linux NetworkManager 默认尝试 PEAP v1，导致握手失败。
2.  **证书验证机制**：图形界面强制要求 CA 证书，但在没有正确证书的情况下无法储存配置。
3.  **密钥环问题**：NetworkManager 有时无法从系统密钥环（Keyring）正确读取密码，导致无限弹窗。

### 🚀 一键修复命令 (推荐)

打开终端 (Terminal)，根据您的网络需求（HKMU 或 eduroam），复制并修改以下命令运行。

#### 连接 "HKMU" (校内常用)

请将 `s1234567` 替换为您的学号，`您的密码` 替换为 Single Password。

```bash
# 1. 清理旧的失败配置
nmcli connection delete HKMU 2> /dev/null

# 2. 创建兼容配置
# 关键参数解释：
# phase1-auth "peapver=0" -> 强制使用旧版 PEAP v0 (核心修复)
# password-flags 0        -> 禁用密钥环，直接储存密码 (解决弹窗循环)
# system-ca-certs no      -> 绕过 CA 证书强制验证
sudo nmcli con add \
  type wifi \
  con-name "HKMU" \
  ssid "HKMU" \
  wifi-sec.key-mgmt wpa-eap \
  802-1x.eap peap \
  802-1x.phase1-auth "peapver=0" \
  802-1x.phase2-auth mschapv2 \
  802-1x.identity "s1234567" \
  802-1x.password "您的密码" \
  802-1x.password-flags 0 \
  802-1x.system-ca-certs no

# 3. 立即连接
nmcli con up HKMU
```

---

✅ **结论**：文档内容已为简体中文，无需翻译。如需进一步润色、英文翻译或格式优化，请告知！
