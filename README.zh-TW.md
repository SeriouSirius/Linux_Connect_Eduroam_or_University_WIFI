# 本指南用於解決 Linux 用戶無法連接 **eduroam** 全球漫遊網絡或 **HKMU** 校園網絡的問題。

[English](README.md) | [繁體中文](README.zh-TW.md) | [簡體中文](README.zh-CN.md)

---

## 1. Eduroam Linux 連接修復指南 (非官方修復方案)

eduroam 的配置要求比普通校園網更嚴格，且經常遇到 Linux 默認協議版本過新、證書校驗無法繞過等問題。本腳本通過強制指定兼容模式來解決這些問題。

### 適用對象
*   HKMU 學生/教職員 (使用 `sxxxxxx@hkmu.edu.hk`)
*   其他高校使用 eduroam 的 Linux 用戶 (只需替換賬號後綴即可)

### ⚡ 核心修復原理
1.  **PEAP Version 0**: 強制降級協議版本，解決舊式 Radius 伺服器握手失敗的問題。
2.  **No CA Cert**: 繞過 Linux 圖形介面強制要求上傳證書的限制。
3.  **Password Flags**: 禁用金鑰環儲存，防止因權限問題導致的無限密碼彈窗。

### 🚀 快速連接命令

請在終端 (Terminal) 中**完整複製**以下命令塊。

**⚠️ 注意：運行前請手動修改命令中的 `您的學號@hkmu.edu.hk` 和 `您的密碼`。**

```bash
# 1. 刪除舊的失敗配置 (防止衝突)
nmcli connection delete eduroam 2> /dev/null

# 2. 創建兼容性極佳的 eduroam 連接
# 注意：eduroam 的 identity 必須包含完整的域名後綴 (如 @hkmu.edu.hk, @polyu.edu.hk 等)
sudo nmcli con add \
  type wifi \
  con-name "eduroam" \
  ssid "eduroam" \
  wifi-sec.key-mgmt wpa-eap \
  802-1x.eap peap \
  802-1x.phase1-auth "peapver=0" \
  802-1x.phase2-auth mschapv2 \
  802-1x.identity "s1234567@hkmu.edu.hk" \
  802-1x.password "您的密碼" \
  802-1x.password-flags 0 \
  802-1x.system-ca-certs no

# 3. 啟動連接
nmcli con up eduroam
```

---

## 2. HKMU Wi-Fi Linux 連接指南 (非官方修復方案)

### 🔴 問題現象
*   點擊連接後一直轉圈，最後斷開。
*   反覆彈窗要求輸入密碼，即使密碼正確。
*   圖形介面中「儲存」按鈕為灰色，強制要求上傳 CA 證書。
*   系統日誌 (`journalctl`) 顯示 `Association took too long` 或 `secrets were required but not provided`。

### 🟢 原因分析
1.  **協議版本不匹配**：學校的 RADIUS 伺服器使用較舊的 **PEAP v0** 協議，而現代 Linux NetworkManager 默認嘗試 PEAP v1，導致握手失敗。
2.  **證書驗證機制**：圖形介面強制要求 CA 證書，但在沒有正確證書的情況下無法儲存配置。
3.  **金鑰環問題**：NetworkManager 有時無法從系統金鑰環（Keyring）正確讀取密碼，導致無限彈窗。

### 🚀 一鍵修復命令 (推薦)

打開終端 (Terminal)，根據您的網絡需求（HKMU 或 eduroam），複製並修改以下命令運行。

#### 連接 "HKMU" (校內常用)

請將 `s1234567` 替換為您的學號，`您的密碼` 替換為 Single Password。

```bash
# 1. 清理舊的失敗配置
nmcli connection delete HKMU 2> /dev/null

# 2. 創建兼容配置
# 關鍵參數解釋：
# phase1-auth "peapver=0" -> 強制使用舊版 PEAP v0 (核心修復)
# password-flags 0        -> 禁用金鑰環，直接儲存密碼 (解決彈窗迴圈)
# system-ca-certs no      -> 繞過 CA 證書強制驗證
sudo nmcli con add \
  type wifi \
  con-name "HKMU" \
  ssid "HKMU" \
  wifi-sec.key-mgmt wpa-eap \
  802-1x.eap peap \
  802-1x.phase1-auth "peapver=0" \
  802-1x.phase2-auth mschapv2 \
  802-1x.identity "s1234567" \
  802-1x.password "您的密碼" \
  802-1x.password-flags 0 \
  802-1x.system-ca-certs no

# 3. 立即連接
nmcli con up HKMU
```
---
